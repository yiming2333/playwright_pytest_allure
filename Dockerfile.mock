# 使用和测试容器相同的基础镜像（保持环境一致）
FROM playwright-base:v1.62.0

# 复制所有项目文件（包括 mock_server.py, templates 等）
COPY mock_server.py .
COPY templates templates/
# 暴露 Flask 默认端口
EXPOSE 5000

# 启动 Mock Server（假设你的入口文件是 mock_server.py，里面有 app.run()）
CMD ["python", "mock_server.py"]